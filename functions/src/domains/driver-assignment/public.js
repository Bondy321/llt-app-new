'use strict';

const {
  buildDriverIdentityProfileUpdates,
  claimDriverAuthUid,
  normalizeDriverId,
  resolveDriverAssignment,
} = require('./driverAssignment');

module.exports = {
  buildDriverIdentityProfileUpdates,
  claimDriverAuthUid,
  normalizeDriverId,
  resolveDriverAssignment,
};
