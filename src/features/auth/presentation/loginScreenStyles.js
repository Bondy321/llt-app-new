import { StyleSheet } from 'react-native';
import createLoginScreenStyles from '../../../../screens/styles/LoginScreen.styles';
import { COLORS, FONT_WEIGHT, RADIUS, SHADOWS, SPACING } from './loginPresentationTheme';

export default createLoginScreenStyles({ StyleSheet, COLORS, FONT_WEIGHT, RADIUS, SHADOWS, SPACING });
